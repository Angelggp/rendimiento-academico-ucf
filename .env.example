# Copiar este archivo como .env y ajustar si hace falta.
# .env NO se sube al repo (ver .gitignore).

DB_USER=rendimiento_user
DB_PASSWORD=rendimiento_pass
DB_NAME=rendimiento_academico

# Usada por Prisma en backend/.env (misma base, formato URL)
DATABASE_URL="postgresql://rendimiento_user:rendimiento_pass@localhost:5432/rendimiento_academico?schema=public"

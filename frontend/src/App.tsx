import { Button } from '@/components/ui/button'

function App() {
  return (
    <main className="flex min-h-svh flex-col items-center justify-center gap-4">
      <h1 className="text-3xl font-semibold">Rendimiento Académico UCF</h1>
      <p className="text-muted-foreground">Frontend listo con React + Vite + Tailwind + shadcn/ui</p>
      <Button>Comenzar</Button>
    </main>
  )
}

export default App
